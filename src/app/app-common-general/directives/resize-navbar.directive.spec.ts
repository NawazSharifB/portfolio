import { ResizeNavbarDirective } from './resize-navbar.directive';

describe('ResizeNavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeNavbarDirective();
    expect(directive).toBeTruthy();
  });
});
