import { DisplayCopyButtonDirective } from './display-copy-button.directive';

describe('DisplayCopyButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayCopyButtonDirective();
    expect(directive).toBeTruthy();
  });
});
